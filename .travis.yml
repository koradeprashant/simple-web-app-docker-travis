language: generic
sudo: required

services:
  - docker

before_install:
  - docker build -t pash04/simple-web-app .

#deploy:
#  provider: elasticbeanstalk
#  region: "ap-south-1"
#  app: "simple-web-app"
#  env: "Simplewebapp-env"
#  bucket_name: "elasticbeanstalk-ap-south-1-462623432869"
#  bucket_path: "simple-web-app"
#  on:
#    branch: master
#  access_key_id: $AWS_ACCESS_KEY
#  secret_access_key: $AWS_SECRET_KEY

after_success:
  - docker build -t pash04/simple-web-app .
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push -t pash04/simple-web-app